# 云计算课程设计截图说明总表

> 本文档用于整理当前已保存的实验截图，并说明每张截图在报告中的作用。  
> 截图统一存放路径：`docs/screenshots/`  
> 报告撰写时可参考本文档中的“图名”“截图说明”和“图下注释”。

---

# 一、任务1：本地容器化与镜像上传

## 图 1-1 Docker Compose 启动成功

**截图文件：**

`Docker Compose 启动成功.png`

**截图说明：**

该截图展示了执行 `docker compose up --build` 后，项目中的前端、后端和 Redis 容器服务成功启动。通过该截图可以证明 Docker Compose 能够完成多容器编排，应用本地运行环境搭建成功。

**报告图下注释：**

如图 1-1 所示，使用 Docker Compose 成功启动了 Flask 后端、Nginx 前端和 Redis 数据库容器，说明本地容器化联调环境搭建成功。

---

## 图 1-2 后端 API 返回 status=ok

**截图文件：**

`任务1-后端api返回status-ok.png`

**截图说明：**

该截图展示了浏览器访问 `http://localhost:5000/api/ping` 后，后端返回 JSON 数据，其中包含 `message`、`ping_count` 和 `status=ok` 等字段。该结果说明 Flask 后端服务运行正常，并且能够成功连接 Redis。

**报告图下注释：**

如图 1-2 所示，访问后端 `/api/ping` 接口后返回 `status=ok`，说明 Flask 后端服务运行正常，并且能够连接 Redis 数据库完成访问次数记录。

---

## 图 1-3 前端调用后端成功

**截图文件：**

`任务1-前端调用后端成功.png`

**截图说明：**

该截图展示了浏览器访问 `http://localhost:8080` 前端页面，并点击“测试后端 API”按钮后，页面成功显示后端返回的 JSON 数据。该结果说明前端页面可以通过 Nginx 代理访问 Flask 后端接口。

**报告图下注释：**

如图 1-3 所示，前端页面点击“测试后端 API”按钮后成功显示后端接口返回结果，说明 Nginx 前端与 Flask 后端之间的请求转发正常。

---

## 图 1-4 后端日志收到请求

**截图文件：**

`任务1-后端日志收到请求.png`

**截图说明：**

该截图展示了 Docker 运行日志中后端服务收到 `/api/ping` 请求，并输出当前访问次数。该日志证明前端请求真实到达后端容器，而不是仅仅显示静态页面。

**报告图下注释：**

如图 1-4 所示，后端日志中出现收到 `/api/ping` 请求的记录，说明前端请求已经成功转发到后端服务，后端能够处理请求并更新 Redis 中的访问次数。

---

## 图 1-5 本地 Docker 镜像构建结果

**截图文件：**

`本地 Docker 镜像构建结果.png`

**截图说明：**

该截图展示了本地 Docker 镜像列表中已经存在 `backend:v1` 和 `frontend:v1` 两个镜像。该结果说明后端 Flask 服务和前端 Nginx 页面均已完成本地镜像构建。

**报告图下注释：**

如图 1-5 所示，本地 Docker 镜像列表中已经生成 `backend:v1` 和 `frontend:v1` 两个镜像，说明应用的前端与后端均已完成容器镜像构建。

---

## 图 1-6 SWR 镜像列表

**截图文件：**

`任务1-06-SWR镜像列表..png`

**截图说明：**

该截图展示了华为云 SWR 容器镜像服务中已经存在 `backend` 和 `frontend` 镜像。该结果说明本地构建的镜像已经成功推送到华为云 SWR 私有镜像仓库，为后续 CCE 集群部署提供镜像来源。

**报告图下注释：**

如图 1-6 所示，backend 和 frontend 镜像已经成功上传到华为云 SWR 镜像仓库，后续 CCE 集群可以从该仓库拉取镜像并完成 Kubernetes 部署。

---

# 二、任务2：CCE 集群创建与节点验证

## 图 2-1 CCE 集群概览

**截图文件：**

`任务2-01-CCE集群概览.png`

**截图说明：**

该截图展示了华为云 CCE 集群 `cloud-course-cluster` 的基本信息，包括集群类型、版本和运行状态。该截图用于证明 CCE 集群已经创建成功。

**报告图下注释：**

如图 2-1 所示，已在华为云华东-上海一 Region 创建 `cloud-course-cluster` 集群，集群类型为 CCE Standard，Kubernetes 版本为 v1.34，当前状态为运行中。

---

## 图 2-2 CCE 节点列表

**截图文件：**

`任务2-02-CCE节点列表.png`

**截图说明：**

该截图展示了 CCE 集群节点管理页面中已经创建的 Worker 节点，节点状态为运行中。该截图用于证明 CCE 节点资源创建成功。

**报告图下注释：**

如图 2-2 所示，`cloud-course-cluster` 集群中已创建 2 个 Worker 节点，节点状态均为运行中，说明 CCE 集群节点资源创建成功。

---

## 图 2-3 kubectl 节点 Ready 状态

**截图文件：**

`任务2-03-kubectl节点Ready.png`

**截图说明：**

该截图展示了在 CloudShell 中执行 `kubectl get nodes -o wide` 的结果，可以看到两个 Worker 节点均处于 Ready 状态。

**报告图下注释：**

如图 2-3 所示，通过 CloudShell 执行 `kubectl get nodes -o wide` 命令，可以看到 CCE 集群中两个 Worker 节点均处于 Ready 状态，说明 kubectl 已成功连接集群，节点运行正常。

---

# 三、任务3：Kubernetes 应用部署

## 图 3-1 应用 YAML 部署成功

**截图文件：**

`任务3-01-应用YAML部署成功.png`

**截图说明：**

该截图展示了执行 `kubectl apply -f task3.yaml` 后，Namespace、ConfigMap、Secret、Deployment 和 Service 等 Kubernetes 资源创建成功。

**报告图下注释：**

如图 3-1 所示，通过 `kubectl apply -f task3.yaml` 成功创建了 Namespace、ConfigMap、Secret、Redis、Backend 和 Frontend 等 Kubernetes 资源，说明应用部署配置已成功提交到 CCE 集群。

---

## 图 3-2 Pod 运行状态

**截图文件：**

`任务3-02-Pod运行状态.png`

**截图说明：**

该截图展示了 backend、frontend 和 redis 对应 Pod 均为 `Running`，说明应用容器已经成功运行。

**报告图下注释：**

如图 3-2 所示，backend、frontend 和 redis 对应的 Pod 均已成功运行，其中 backend 副本数为 2，frontend 和 redis 副本数为 1，说明应用容器已成功部署到 CCE 集群中。

---

## 图 3-3 Service NodePort 端口

**截图文件：**

`任务3-03-Service-NodePort端口.png`

**截图说明：**

该截图展示了 backend 服务通过 NodePort 类型暴露，frontend 和 redis 服务通过 ClusterIP 在集群内部访问。

**报告图下注释：**

如图 3-3 所示，backend 服务通过 NodePort 类型暴露，节点端口为 31299 和 31355；frontend 与 redis 服务为 ClusterIP 类型，用于集群内部访问。

---

## 图 3-4 集群内部访问 API 成功

**截图文件：**

`任务3-04-集群内部访问api成功.png`

**截图说明：**

该截图展示了通过临时 curl-test Pod 访问 backend Service 的 `/api/ping` 接口，接口返回 `status=ok`。

**报告图下注释：**

如图 3-4 所示，通过临时 curl-test Pod 访问 backend Service 的 `/api/ping` 接口，成功返回 `status=ok`，说明后端服务在集群内部访问正常，并且能够连接 Redis 完成计数自增。

---

## 图 3-5 ConfigMap 和 Secret

**截图文件：**

`任务3-05-ConfigMap和Secret.png`

**截图说明：**

该截图展示了 `backend-config`、`redis-secret` 和 `swr-secret` 已经创建成功。

**报告图下注释：**

如图 3-5 所示，`cloud-course` 命名空间中已创建 `backend-config`、`redis-secret` 和 `swr-secret`。其中 `backend-config` 用于保存后端连接 Redis 的配置，`redis-secret` 用于保存 Redis 密码，`swr-secret` 用于拉取 SWR 私有镜像。

---

## 图 3-6 Deployment 副本状态

**截图文件：**

`任务3-06-Deployment副本状态.png`

**截图说明：**

该截图展示了 backend、frontend 和 redis 的 Deployment 副本状态均符合预期。

**报告图下注释：**

如图 3-6 所示，backend Deployment 期望副本数为 2，当前 2 个副本均处于 Ready 状态；frontend 和 redis 均为 1 个副本且运行正常，说明应用部署副本状态符合预期。

---

# 四、任务4：Redis PVC 持久化

## 图 4-1 PVC 绑定成功

**截图文件：**

`任务4-01-PVC绑定成功.png`

**截图说明：**

该截图展示了 `redis-pvc` 状态为 Bound，说明 Redis 持久化存储卷已经创建并绑定成功。

**报告图下注释：**

如图 4-1 所示，`redis-pvc` 的状态为 Bound，容量为 5Gi，StorageClass 为 csi-disk，说明 Redis 持久化存储卷已成功创建并绑定到云硬盘。

---

## 图 4-2 Redis 写入和读取数据

**截图文件：**

`任务4-02-Redis写入和读取数据.png`

**截图说明：**

该截图展示了通过 `redis-cli` 向 Redis 写入 `testkey=hello-pvc`，并读取该数据进行验证。

**报告图下注释：**

如图 4-2 所示，通过 `redis-cli` 向 Redis 写入测试键值 `testkey=hello-pvc`，并成功读取到对应结果，说明 Redis 数据读写正常。

---

## 图 4-2 Redis 写入数据

**截图文件：**

`任务4-02-Redis写入数据.png`

**截图说明：**

该截图展示了 Redis 写入命令返回 OK，可作为写入数据的补充证明。

**报告图下注释：**

如图 4-2 所示，Redis 写入命令返回 OK，说明测试数据已经成功写入 Redis。

---

## 图 4-3 删除 Redis Pod

**截图文件：**

`任务4-03-删除RedisPod.png`

**截图说明：**

该截图展示了手动删除 Redis Pod 的过程，用于验证 Deployment 自动重建和 PVC 持久化能力。

**报告图下注释：**

如图 4-3 所示，手动删除 Redis Pod 后，Kubernetes Deployment 会自动重新创建新的 Redis Pod，为后续验证持久化数据恢复做准备。

---

## 图 4-4 Redis Pod 重建

**截图文件：**

`任务4-04-RedisPod重建.png`

**截图说明：**

该截图展示了 Redis Pod 删除后，Kubernetes 自动重建了新的 Redis Pod，并且状态为 Running。

**报告图下注释：**

如图 4-4 所示，原 Redis Pod 删除后，Deployment 自动创建了新的 Redis Pod，且新 Pod 状态为 Running，说明 Kubernetes 具备自动恢复能力。

---

## 图 4-5 数据恢复验证

**截图文件：**

`任务4-05-数据恢复验证.png`

**截图说明：**

该截图展示了 Redis Pod 删除并重建后，仍然能够读取到之前写入的 `hello-pvc` 数据，说明 PVC 持久化生效。

**报告图下注释：**

如图 4-5 所示，删除 Redis Pod 后重新读取 `testkey`，仍然返回 `hello-pvc`，说明 Redis 数据已通过 PVC 持久化保存，Pod 重建后数据未丢失。

---

# 五、任务5：ConfigMap Volume 挂载

## 图 5-1 nginx ConfigMap 配置

**截图文件：**

`任务5-01-nginx-configmap配置.png`

**截图说明：**

该截图展示了 `nginx-config` ConfigMap 的创建和配置内容。

**报告图下注释：**

如图 5-1 所示，`nginx-config` ConfigMap 中保存了 Nginx 的 `default.conf` 配置文件内容，为后续通过 Volume 挂载到 frontend Pod 提供配置来源。

---

## 图 5-1 nginx ConfigMap 配置补充

**截图文件：**

`任务5-01-nginx-configmap配置2.png`

**截图说明：**

该截图展示了更新后的 Nginx 配置内容，其中 `/api/` 请求被反向代理到 backend 服务。

**报告图下注释：**

如图 5-1 所示，Nginx 配置中已添加 `/api/` 反向代理规则，请求会被转发到 `backend.cloud-course.svc.cluster.local`，用于实现前端到后端的服务转发。

---

## 图 5-2 frontend Volume 挂载后 Pod 运行

**截图文件：**

`任务5-02-frontend-volume挂载后Pod运行.png`

**截图说明：**

该截图展示了 frontend Deployment 更新后，新的 frontend Pod 处于 Running 状态。

**报告图下注释：**

如图 5-2 所示，frontend Deployment 更新后重新创建的 Pod 已处于 Running 状态，说明 ConfigMap Volume 挂载配置未影响前端服务正常运行。

---

## 图 5-3 Pod 内查看 nginx 配置

**截图文件：**

`任务5-03-Pod内查看nginx配置.png`

**截图说明：**

该截图展示了进入 frontend Pod 后，可以查看到 `/etc/nginx/conf.d/default.conf` 文件内容，说明 ConfigMap 成功挂载到容器内部。

**报告图下注释：**

如图 5-3 所示，进入 frontend Pod 后可以查看到 `/etc/nginx/conf.d/default.conf` 文件，说明 `nginx-config` 已通过 ConfigMap Volume 的方式成功挂载到容器中。

---

## 图 5-4 前端 Service 访问验证

**截图文件：**

`任务5-04-前端Service访问验证.png`

**截图说明：**

该截图展示了通过临时 curl Pod 访问 frontend Service 后，成功返回前端 HTML 页面内容。

**报告图下注释：**

如图 5-4 所示，通过临时 curl Pod 访问 frontend Service 后成功返回前端 HTML 页面内容，说明 frontend Service 在集群内部访问正常。

---

## 图 5-5 前端反向代理 API 成功

**截图文件：**

`任务5-05-前端反向代理api成功.png`

**截图说明：**

该截图展示了通过 frontend Service 访问 `/api/ping` 后，Nginx 成功将请求反向代理到 backend，并返回 `status=ok`。

**报告图下注释：**

如图 5-5 所示，通过临时 curl Pod 访问 frontend Service 的 `/api/ping` 路径，成功返回 `status=ok`，说明 frontend 中的 Nginx 反向代理配置生效，前端服务能够将 API 请求转发到 backend 服务。

---

# 六、任务6：HPA 弹性伸缩

## 图 6-1 metrics 可用

**截图文件：**

`任务6-01-metrics可用.png`

**截图说明：**

该截图展示了 `kubectl top nodes` 可以正常返回节点 CPU 和内存指标，说明 metrics 指标服务可用。

**报告图下注释：**

如图 6-1 所示，执行 `kubectl top nodes` 后可以查看两个 Worker 节点的 CPU 和内存使用率，说明集群资源指标服务已正常工作，为后续 HPA 弹性伸缩提供了指标基础。

---

## 图 6-2 HPA 创建成功

**截图文件：**

`任务6-02-HPA创建成功.png`

**截图说明：**

该截图展示了 backend Deployment 已成功创建 HPA，最小副本数为 2，最大副本数为 5，目标 CPU 使用率为 30%。

**报告图下注释：**

如图 6-2 所示，backend Deployment 已成功创建 HPA，最小副本数为 2，最大副本数为 5，目标 CPU 使用率为 30%，用于根据负载自动调整后端 Pod 副本数量。

---

## 图 6-3 压测 Pod 运行

**截图文件：**

`任务6-03-压测Pod运行.png`

**截图说明：**

该截图展示了 `load-generator` 压测 Pod 已成功运行，并持续访问 backend 服务制造负载。

**报告图下注释：**

如图 6-3 所示，创建 `load-generator` Pod 后，该 Pod 持续访问 backend 服务的 `/api/ping` 接口，用于模拟请求压力，为 HPA 自动扩容提供负载。

---

## 图 6-4 Pod 扩容截图

**截图文件：**

`任务6-04-Pod扩容截图.png`

**截图说明：**

该截图展示了在压测过程中，HPA 检测到 CPU 使用率超过目标值，并将 backend 副本数扩容到 5 个。

**报告图下注释：**

如图 6-4 所示，在 `load-generator` 压测 Pod 持续访问 backend 服务后，backend 的 CPU 使用率超过 HPA 设置的 30% 目标值，因此 HPA 将 backend 副本数自动扩容到 5 个，说明弹性伸缩配置生效。

---

## 图 6-5 Pod 缩容截图

**截图文件：**

`任务6-05-Pod缩容截图.png`

**截图说明：**

该截图展示了停止压测后，backend CPU 使用率降低，HPA 将副本数缩回到 2 个。

**报告图下注释：**

如图 6-5 所示，停止压测后，backend 的 CPU 使用率下降到较低水平，HPA 将 backend 副本数从 5 个缩回到 2 个，说明 HPA 能够根据负载变化完成自动缩容。

---

## 图 6-6 HPA 详情

**截图文件：**

`任务6-06-HPA详情.png`

**截图说明：**

该截图展示了 HPA 的详细配置和 Events 事件，其中包含 SuccessfulRescale 记录，说明 HPA 实际执行过扩容操作。

**报告图下注释：**

如图 6-6 所示，backend 的 HPA 最小副本数为 2，最大副本数为 5，目标 CPU 使用率为 30%。Events 中出现 SuccessfulRescale 事件，说明 HPA 在压测过程中根据 CPU 使用率变化自动调整了 backend 副本数量。

---

# 七、截图总结

通过以上截图可以证明，本课程设计已完成从本地容器化、SWR 镜像上传、CCE 集群创建、Kubernetes 应用部署、Redis PVC 持久化、ConfigMap Volume 挂载到 HPA 弹性伸缩的完整流程。各截图分别对应实验过程中的关键验证点，可作为课程设计报告中实验步骤、实验结果和结果分析的支撑材料。
'@ | Set-Content docs\report-notes\截图说明总表.md -Encoding UTF8